# Environment Variables Template
# Copy this file to .env and update the values as needed

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL Database Connection Settings
# Used for connecting to the PostgreSQL database server
POSTGRES_USER=boba                  # Database username
POSTGRES_PASSWORD=boba              # Database password
POSTGRES_DB=boba                    # Database name
POSTGRES_HOST=db                    # Database host (use 'db' for Docker, 'localhost' for local)
POSTGRES_PORT=5432                  # Database port (default PostgreSQL port)

# Example database URL format (alternative to individual variables):
# DATABASE_URL=postgresql://boba:boba@db:5432/boba

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Add other environment variables here as needed
# NODE_ENV=development
# API_BASE_URL=http://localhost:3000
# JWT_SECRET=your-secret-key

# =============================================================================
# MCP SERVER CONFIGURATION
# =============================================================================

# Serena MCP Configuration
UVX_PATH=/Users/y-kubo/.local/bin/uvx    # Absolute path to uvx command
SERENA_PROJECT_PATH=/Users/y-kubo/.dotfiles  # Default project path for Serena MCP

# Chrome DevTools MCP Configuration
CHROME_HEADLESS=false                    # Run Chrome in headless mode (true/false)
CHROME_VIEWPORT=1280x720                 # Initial viewport size (WIDTHxHEIGHT)
CHROME_DEBUG_PORT=9222                   # Chrome debug port number