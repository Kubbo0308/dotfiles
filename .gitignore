# macOS files
.DS_Store
*.swp
*.bak

# Local environment specific settings
local/
claude/settings.local.json
*.local
_docs/
*.gif

# Confidential files - Claude settings and sensitive data
claude/settings.json
claude/projects/*/
claude/todos/
claude/.claude/
claude/statsig/
claude/ide
claude/shell-snapshots/
claude/plugins/config.json.backup
log/

# API keys and tokens
*.key
*.pem
*.p12
*.token
*_token
*_key
api_keys.json
secrets.json

# Environment variables with sensitive data
.env.local
.env.production

# SSH keys and configs
ssh/id_*
ssh/known_hosts
ssh/config

# GPG keys
gnupg/

# Oh-My-Zsh - ignore the main directory but allow custom configs
oh-my-zsh/
!oh-my-zsh-custom/

# Neovim temporary files
nvim/lazy-lock.json
nvim/*.tmp
nvim/swap/
nvim/backup/
nvim/undo/

# Wezterm logs and temporary files
wezterm/.claude/
wezterm/logs/
