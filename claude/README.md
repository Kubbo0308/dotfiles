# Claude Code Configuration

**Single source of truth** for all Claude Code configuration. This directory (`claude/`) is symlinked as `~/.claude` from the home directory.

> **Note:** A `.claude/` directory may exist at the repository root, but it is gitignored and auto-generated by Claude Code at runtime. All persistent configuration lives here.

## Directory Structure

```
claude/                      # Symlinked as ~/.claude
├── CLAUDE.md                # Global development standards and instructions
├── settings.json            # Main settings (hooks, permissions, env)
├── statusline.js            # Custom status line script
├── agents/                  # Subagent definitions (29)
├── commands/                # Custom slash commands (38)
├── skills/                  # Development skills (18)
├── hooks/                   # Lifecycle hooks (11 scripts)
├── teams/                   # Team definitions for orchestration (4 YAML + 1 custom)
├── mcp/                     # Model Context Protocol servers
├── plugins/                 # Plugin management
├── projects/                # Project-specific settings
├── environments/            # Environment variables
├── session-env/             # Session environments
├── plans/                   # Saved plans
├── tasks/                   # Task tracking
├── configs/                 # Additional configs
├── ide/                     # IDE integrations
└── cache/                   # Cache files
```

## Subagent First Policy

> **Before every action, verify: Can a Subagent handle this?**

| Priority | Agent | Purpose |
|----------|-------|---------|
| **ALWAYS** | `serena-context` | Load project context at session start |
| **ALWAYS** | `codebase-analyzer` / `Explore` | Analyze codebase structure |
| **ALWAYS** | `pre-commit-checker` | Review changes before commit |
| **HIGH** | `commit` | Generate quality commit messages |
| **HIGH** | `pull-request` | Create well-documented PRs |
| **HIGH** | `code-reviewer-gemini` | AI-powered code review |
| **HIGH** | `typescript-developer` / `go-developer` | Language-specific development |
| **HIGH** | `typescript-test-generator` | Generate TypeScript/React tests |
| **MEDIUM** | `security` | Security analysis |
| **MEDIUM** | `web-researcher` | Technical research |
| **MEDIUM** | `document` | Documentation generation |
| **MEDIUM** | `task-decomposer` | Break down complex tasks |
| **MEDIUM** | `drawio-diagram-generator` | Create architecture diagrams |

### Specialized Reviewers

| Agent | Focus Area |
|-------|------------|
| `go-reviewer` | Go idiomatic patterns |
| `typescript-reviewer` | TypeScript/React best practices |
| `terraform-reviewer` | Infrastructure as Code |
| `dbt-reviewer` | dbt SQL style and testing |
| `markdown-reviewer` | Documentation quality |
| `clean-code-fp-reviewer` | Clean code & functional programming |
| `codex-reviewer` | OpenAI Codex cross-agent review |
| `code-reviewer-cursor` | Cursor AI code review |

### MAGI System

Three-agent deliberation system for complex decisions:

| Agent | Role |
|-------|------|
| `magi-melchior` | Scientific/logical analysis |
| `magi-balthasar` | Practical implementation focus |
| `magi-casper` | Risk assessment and caution |

Invoke with `/magi <question>` command.

## Skills (18)

Skills provide domain-specific knowledge and patterns. See [skills/README.md](skills/README.md).

## Commands (38)

Custom slash commands for development workflows. See [commands/README.md](commands/README.md).

## Hooks (11 scripts)

Lifecycle hooks for security, linting, and workflow automation. See [hooks/README.md](hooks/README.md).

## Teams

Team definitions for orchestrated multi-agent workflows. See [teams/README.md](teams/README.md).

## MCP Servers

See [mcp/README.md](mcp/README.md) for detailed MCP configuration.

Configured servers: Serena, Playwright, Chrome DevTools, Context7, Filesystem, PostgreSQL.

## Quick Reference

### Before Any Commit

```
@pre-commit-checker  →  @commit
```

### Before Any PR

```
@code-reviewer-gemini  →  @pull-request
```

### For Code Understanding

```
@codebase-analyzer  or  @Explore
```
